name: admin
version: v1.0

discovery:
  etcdEndpoint: http://etcd-server:2379
  etcdUser: root
  etcdPassword: sabertest
  dialTimeout: 5s
  autoSyncInterval: 10s
  dialKeepAliveTime: 5s
  dialKeepAliveTimeout: 5s
  registryRootKeyPrefix: /os-artificer/saber
  registryTTL: 60

apm:
  enabled: true
  endpoint: tcp://127.0.0.1:8202

service:
  listenAddress: tcp://127.0.0.1:26690

log:
  fileName: ./logs/admin.log
  logLevel: debug
  fileSize: 100
  maxBackupCount: 10
  maxBackupAge: 10
