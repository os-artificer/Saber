{{- if and .Values.agent.enabled .Values.agent.daemonSet }}
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: {{ include "saber.agent.fullname" . }}
  labels:
    {{- include "saber.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
spec:
  selector:
    matchLabels:
      {{- include "saber.agent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "saber.agent.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: agent
          image: {{ include "saber.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - /app/agent
          args:
            - -c
            - /config/agent.yaml
          volumeMounts:
            - name: config
              mountPath: /config
              readOnly: true
          {{- with .Values.agent.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "saber.agent.fullname" . }}
      {{- with .Values.agent.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.agent.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
